version: '3.8'

services:
  bot:
    build: .
    container_name: mega_bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - ./downloads:/app/downloads
